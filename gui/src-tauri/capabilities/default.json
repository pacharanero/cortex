{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window. The app's own commands need no capability entries; this grants only the debug-build MCP bridge used for automated UI driving.",
  "windows": ["main"],
  "permissions": ["mcp-bridge:default"]
}