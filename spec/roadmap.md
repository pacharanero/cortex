---
afx: true
type: SPEC
status: Living
owner: "@pacharanero"
version: "1.0"
created_at: "2026-08-01T17:30:00.000Z"
updated_at: "2026-08-01T17:30:00.000Z"
tags: ["roadmap", "planning"]
---

# cortex-rs Roadmap

> Stable IDs, never renumbered - reference them in commits, PRs, and release notes.
>
> Status legend: `[x]` done, `[~]` in-progress, `[ ]` planned. Future items live under `## Future` and promote to Planned when scheduled.
>
> Completed parent items move to `completed.md`; completed sub-items remain beside unfinished siblings while an active milestone is mixed. `s/progress` counts both files. Once a CHANGELOG exists, this roadmap becomes a pure backlog.
>
> **`[x]` means the code exists and passes the local gate. It does NOT mean hardware-verified.** Anything touching the wire stays provisional until its hardware smoke item passes against a real Quad Cortex.
>
> Hardware-verified so far (through 2026-08-11, CorOS 4.0.1): transport through the implemented typed client; the held daemon, live cache and auto-managed idle release; read, navigation, grid, typed routing with mandatory host read-back, tempo, STOMP/expression/MIDI, file, capture/IR selection, global-setting and I/O paths; pinning/favourites; physical reconnect; and the native GUI's fail-closed reconnect rendering. Individual items below identify what remains provisional or unimplemented.
>
> **Overnight routing:** `Night: ready` marks a bounded task that can be completed without USB hardware. `Night: slice` permits only the explicitly named offline subset and requires a PR `Hardware follow-up` section where applicable. Unmarked items are not available to the overnight routine. The copy-paste routine prompt and its safety rules live in [overnight-routine.md](overnight-routine.md).

## Next Milestone: Daemon-backed GUI Reads

Connect the existing Tauri first draft to the same held-session contract already proven by CLI and MCP, without widening the write surface:

1. [Done] Write the as-built GUI design and make `cortex-host` the explicit Tauri backend boundary.
2. [Done] Replace fixture-only status, preset directory, live grid, active scene and CPU data with typed daemon snapshots and revisions.
3. [Done] Surface connected/reconnecting/failed state and never render a stale generation as live.
4. [Done] Keep fixture mode as a deliberate development/test adapter rather than hidden fallback behavior.
5. [In progress] Boundary-focused Rust tests, both frontend builds, browser fixture smoke, the real-device dashboard boundary and physical unplug/reconnect pass. Automated native DOM/IPC checks through Tauri MCP remain.

Saving remains outside this milestone. GUI save needs exact-target preparation and confirmation UX, restoration semantics, typed failures and its own hardware smoke. In parallel, CLI-004.4 remains the next distribution milestone: a Linux x86_64 preview containing both `cortex` and `cortex-mcp`.

## Where each ID lives

This file is the single place to see where the project is up to. Each ID names work; the zone spec beside it says what that work must do and how it is designed.

| ID prefix | Zone | What it covers |
| --- | --- | --- |
| PROT-001 | [100-transport](100-transport/spec.md) | USB HID open, read/write, the benign write STALL |
| PROT-002 | [110-framing](110-framing/spec.md) | Report framing, flag-driven reassembly, the trailer envelope |
| PROT-003 | [120-proto-schema](120-proto-schema/spec.md) | Vendored `.proto`, `prost` build, message types |
| PROT-004 | [130-domain-model](130-domain-model/spec.md) | `DeviceKind`, `Row`, `Catalog`, and the typed preset/grid views |
| PROT-005 | [140-session](140-session/spec.md) | Handshake, keepalive, correlation, broadcast waiting |
| PROT-006 | [150-client](150-client/spec.md) | The ergonomic `QuadCortex` API |
| PROT-007 | [150-client](150-client/spec.md) | Capture and IR export/import (investigation) |
| CLI-00x | [200-cli](200-cli/spec.md) | The `cortex` command surface |
| MCP-00x | [300-mcp](300-mcp/spec.md) | The MCP server and its safety boundary |
| GUI-00x | [400-gui](400-gui/spec.md) | The Tauri desktop app |
| DOCS-00x | - | The documentation site and the agent-facing model reference |
| ENG-001 | [500-dx-tooling](500-dx-tooling/spec.md) | Scripts, lint, test |
| ENG-002 | [600-ci-release](600-ci-release/spec.md) | CI and release |
| ENG-003 | [900-project-governance](900-project-governance/spec.md) | Licensing, attribution, legal hygiene |
| ENG-004 | [001-overview](001-overview/spec.md) | Traceability |
| ENG-005 | - | `s/usb-trace`, for observing the official client on the wire |

!!! tip "Check [prior-art.md](prior-art.md) before implementing a protocol item"

    Many unticked PROT items already have an implementation, captured wire shape, or documented investigation in `pyquadcortex`, and several carry silent-failure traps that cost that project real time. [prior-art.md](prior-art.md) records what the prior-art projects know that we do not, the verification level of those findings, and which negative results are worth re-testing. Items below cite it where it applies.

!!! note

    Progress is tracked HERE and nowhere else. Zone folders hold `spec.md` (what it must do) and `design.md` (how it is built and why), but no `tasks.md` - see [001-overview/spec.md](001-overview/spec.md#progress-tracking) for why that convention was dropped.

## Protocol and Crate (PROT)

The bottom-up port of the Cortex Control USB HID protocol from `pyquadcortex` (MIT) into a Rust leaf crate.

### PROT-006: Client API (zone 150)

The ergonomic `QuadCortex` struct - the Rust equivalent of pyquadcortex's 60+ methods.

!!! note "Visual verification closure for PROT-006.12"

    Gig View visibly opened and closed from the recovered update on 2026-08-26. The same restoration-first smoke sent `ShowTuner{UPDATE, show:true}` twice without a visible Tuner; repeating the update after an explicit `ShowTuner{READ}` subscription also produced no visible change in two runs. A synchronized normal subscribed session observed no `ShowTuner` message when the Tuner was physically closed and reopened. The public method now fails before I/O rather than reporting false success. Capturing a working official-client shape is separate work under PROT-006.19.

    Two traps apply throughout. **A nested submessage write replaces the whole submessage** - send one flag of a group and its siblings go false, which in one upstream case quietly stopped the Master Volume knob governing most outputs. Read, merge, write. And **the action field is load-bearing**: some operations need `CREATE`, some `READ`, some no action at all, and the wrong one is ignored in silence rather than refused.

- [x] **PROT-006.12**: The automated restoration-first General Settings, Master Volume assignment, global Cab/IR bypass, Global EQ, mode/cycle and Tuner settings test passed in full on CorOS 4.0.1, including complete final baseline restoration. Value 9 and every power/reset/updater/internal-clock shape remain structurally absent. **Visual closure 2026-08-26:** Gig View visibly opened and closed. The recovered `ShowTuner{UPDATE, show}` shape produced no visible effect with or without an explicit `ShowTuner{READ}` subscription, and physical Tuner close/reopen produced no unsolicited `ShowTuner` push, so `show_tuner` now returns unsupported before I/O pending PROT-006.19
- [x] **PROT-006.16**: Hardware smoke coverage is expanded through every implemented non-UI PROT-006 path on CorOS 4.0.1. The 2026-08-10 runs added wider reads; row controls; all eight tempo/metronome methods; STOMP/expression and persistent MIDI; generated-setlist file composition; capture and user-IR selection; automated global settings; the complete I/O matrix and mutations; and pin/Favorite restoration. All mutable runs restored their baselines and cleaned generated storage. The 2026-08-26 visual pass verified Gig View and converted the measured no-op Tuner visibility path into an explicit refusal; the unsupported Capture-dialog response remains deliberately descoped to FUTURE-008
- [x] **PROT-006.17**: **Deliberate non-feature.** Capture creation, dialog handling, transfer, backup, and cloud workflows remain native-device workflows. Selecting an existing device capture in a grid block remains a supported tone-building operation. V2 capture creation requires Neural DSP cloud processing, and a host-owned workflow would add protocol and safety risk without advancing the product's agent-control goal. `show_dialog: true` remains structurally unavailable from the public client and absent from CLI, MCP and GUI; do not infer or send `show_dialog_fail_reason`
- [ ] **PROT-006.18**: Establish whether USB can select a grid block or open its on-device editing panel. The recovered schema has no explicit focus/panel command; `GridModelMeter` type 37 is the only non-editing message carrying row and column and may merely select the meter source. Trace short Cortex Control actions separately: desktop block A/B selection without edits, close/deselect, matching hardware taps, and a known parameter edit plus scene-switch positive controls. If selection is desktop-local, record that negative result rather than inventing a command; if type 37 correlates, capture open/close and cross-coordinate shapes before any replay
- [ ] **PROT-006.19**: Capture Cortex Control opening and closing the on-device Tuner before restoring host-controlled visibility. On CorOS 4.0.1, `ShowTuner{UPDATE, show:true}` returned transport success but produced no visible change in two ordinary runs or two runs preceded by `ShowTuner{READ}`; physically closing and reopening Tuner emitted no unsolicited `ShowTuner` push to a normal subscribed session. Do not guess another action or conflate Tuner settings (`Tuner`) with screen visibility (`ShowTuner`); re-enable the public method only after a captured host-to-device shape visibly works

### PROT-007: Capture and IR export / import

Neural Captures and user IRs live only on the unit and in Neural's cloud. No existing tool - official or community - can export a capture to a local file, so a player's own captures cannot currently be backed up, version-controlled, or moved between units.

Status: **deliberate non-feature, not a release blocker.** Capture and IR creation, backup, transfer, and cloud processing remain native Neural DSP workflows. Selecting existing captures and IRs in compatible grid blocks remains in scope. The product focus is safe agent-driven control of the Quad Cortex through MCP, not duplicating the vendor's capture lifecycle or attempting to decrypt opaque user data. Keep the existing protocol evidence as a research record, but do not resume implementation without a new explicit product decision. The investigation established that Cortex Control 4.0.1 returns a complete opaque whole-unit backup through chunked `LocalBackup{UPDATE}` messages after a `LocalBackup{CREATE}` request, but does not establish individual capture export or backup contents. The file category is independently solved: `FileMessage.type` is 0 for presets, 1 for IRs, and 2 for captures. `FileMessage` carries `ir_payload` and `preset_payload` `bytes` fields, but no per-capture payload read has been observed.

!!! warning "Known constraints before tracing"

    Upstream hardware work mapped a candidate IR-import request, established that `total_bulk_create_count` is mandatory, and round-tripped an outbound write spanning 26 HID reports. Its imports still produced no IR, so payload content, action choice, and other request semantics remain unknown; outbound framing itself is proven. Repeated multi-kilobyte attempts coincided with the USB link dying until a power cycle; space destructive probes out ([prior-art.md](prior-art.md#on-captures-and-irs-prot-007)).

    Separately, reference-only pre-CorOS-3 prior art without a repository-wide licence reports captures as encrypted protobufs and local captures as serial-number-keyed. That is a provisional lead to test, not source material or a current-format fact; if true, cross-unit import may be impossible without a portable representation ([prior-art.md](prior-art.md#opencortex---one-high-value-fact-and-a-minefield)).

- [~] **PROT-007.1**: A complete whole-unit backup is HARDWARE-MEASURED over `LocalBackup` on CorOS 4.0.1. Cortex Control sends `CREATE` with only a request id; the device returns ordered `UPDATE` chunks carrying JSON text and an explicit final-chunk flag. One backup delivered 1,016,596 JSON bytes in seven messages over 8,072 HID reports. **Exact residual:** establish whether the opaque backup actually contains every Neural Capture and user IR, or observe a separate per-item payload route; whole-backup transport alone does not prove individual capture export
- [~] **PROT-007.2**: The outer container is identified: concatenated `LocalBackup.backup_json` is a metadata JSON object with a Base64 `payload` and 64-hex `payload_hash`. The measured Base64 decoded to 762,184 opaque bytes with no gzip/archive signature, almost no printable text and 7.999772 bits/byte Shannon entropy, consistent with encryption. The documented pre-CorOS-3 no-serial decrypt path did not decode this CorOS 4.0.1 payload. **Exact residual:** establish the current cipher and key scope, validate `payload_hash`, decrypt the content, then determine whether compression exists inside the encrypted layer
- [x] **PROT-007.3**: **Deliberate non-feature.** No capture export implementation
- [x] **PROT-007.4**: **Deliberate non-feature.** No capture import implementation
- [x] **PROT-007.5**: **Deliberate non-feature.** No user-IR export/import implementation
- [x] **PROT-007.6**: **Deliberate non-feature.** No capture or IR transfer CLI surface
- [x] **PROT-007.7**: **Not applicable.** No transfer container format without a transfer feature

Do not ship import before export has been round-tripped on hardware: writing a malformed capture to the unit is the most plausible way this project could damage a user's data.

### PROT-009: Correctness gaps found in review, 2026-08-05

Raised by a review of the state-cache/prepared-save/daemon-routing change (`a603bce`). The tree was green - fmt, clippy, `reuse`, and 149 tests all passed - so every item here is a behavioural gap that the existing tests did not reach. All claims have now been checked; completed items state their verification level.

Ordered by what would hurt a user most.

- [~] **PROT-009.14**: **Regression coverage.** Offline coverage now includes explicit link-drop with retained session references; exclusivity-aware responsive-gap reconnect; prepared-save phase matrix and generation/storage epoch; concurrent public-flow File list/save/delete waiters with wrong targets and reply-ordered completion; exhaustive malformed 256-entry listing rejection (missing, negative, 256+, duplicate, wrong action/type/folder and combined malformed shapes); recall/edit/save ordering; stale-empty target backup; partial File invalidation; malformed envelopes; spawned older/newer daemon protocol skew with cross-version shutdown; parameter kinds and real-unit conversion. Tests and protocol docs pin two irreducible limits without claiming a fix: delayed acknowledgements from an earlier identical File operation are wire-indistinguishable because no usable request ID exists, and the final listing-check/write interval has no revision or compare-and-swap guard. No `File SWAP` is implemented absent evidence. **Exact residual:** hardware confirmation that a naturally occurring malformed report or envelope triggers replacement-handshake recovery and a healthy next request. Do not inject malformed traffic into hardware; if no natural fault is observed, retain this as unexercised. Rerun strict concurrent File operations on hardware only if future CorOS behavior suggests broadcasts differ from the measured shapes. Parent remains partial because its original acceptance included hardware fault recovery

## Docs (DOCS)

### DOCS-002: Agent manual - factory preset reference

A per-device, per-CorOS reference of the factory presets: what each is modelled on, and how to get a usable sound out of it. Aimed at agents driving the MCP server, who otherwise have no idea that "Brit 2203" is a Marshall-style voicing.

- [ ] **DOCS-002.1**: Generate the raw preset inventory from the device rather than transcribing it - `cortex preset list --setlist "/opt/neuraldsp/Factory Library"` already emits all 256 names. Keep it a build step so a CorOS update regenerates it
- [ ] **DOCS-002.2**: Key the reference by device AND CorOS version; factory content changes between releases and a stale mapping is worse than none
- [ ] **DOCS-002.3**: Setup tips per preset - the genuinely additive part, since the gear mapping is no longer ours to write (see below)
- [ ] **DOCS-002.4**: Expose it to the MCP server so an agent can resolve intent ("something like a cranked Plexi") to a slot

**This item shrank substantially on 2026-08-02.** The device's own catalog carries a `tm` attribute holding **Neural DSP's own attribution** for each model - `Based on Marshall(R) JCM800(R)`, `Based on ProCo(R) Rat(R)`, `Based on Universal Audio(R) 1176(R)` - on 318 of 533 models. So the "what is this modelled on" mapping does not need writing at all: it ships with the unit, in the vendor's own carefully-worded form, and `cortex catalog --search marshall` already surfaces it.

That changes the plan in three ways:

1. **Never paraphrase the `tm` string.** Reproduce it verbatim. It is Neural DSP's statement about other companies' marks; rewording it, or presenting our own mapping as authoritative, is both less accurate and less defensible. The crate surfaces it as `Model::based_on` and the CLI prints it unchanged.
2. **The remaining work is genuinely additive only** - setup tips, and mapping *presets* (which the `tm` data does not cover) to the *models* they contain, which the catalog does let us resolve.
3. **This is a runtime lookup, not a document to write.** An agent can already resolve "something like a cranked Plexi" by searching the live catalog, which stays correct across CorOS updates for free.

Remaining constraints:

- **Trademark caveating is still mandatory for anything WE write.** Fender, Marshall, Mesa/Boogie, Vox and the rest are other companies' marks. Follow the industry norm every modelling vendor and community site uses: describe what a preset is *evocative of*, never imply endorsement, licensing, or that the model IS the amp. Neural DSP's own naming is deliberately oblique ("Brit 2203", not "Marshall JCM800") and our docs should not undo that by publishing a decode table presented as authoritative.
- **Preset NAMES are factual interoperability information; preset DATA is Neural DSP's.** Listing names and our own commentary is fine. Committing extracted factory preset payloads into this repo is not - see the legal hygiene section of AGENTS.md.

## CLI (CLI)

The `cortex` command-line surface over the crate.

### CLI-002: Format and output

- [x] **CLI-002.3**: `cortex --schema --format json` emits the bounded agent-operation input registry shared with MCP (CLI-007.1)

### CLI-003: Preset and scene commands

The implemented preset/scene command surface is hardware-verified against CorOS 4.0.1. `scene switch|label|unlabel|color|copy|swap` use noun-then-verb naming; the shipped `scene --index` switch shorthand remains accepted.

- [x] **CLI-003.9**: **Deliberate non-feature.** Capture and IR transfer remain native-device workflows (PROT-007)

### CLI-004: Distribution

**Decision (2026-08-07):** the primary user channel is a Linux x86_64 preview containing both `cortex` and `cortex-mcp`, installed without Rust or `protoc`, followed by one guided udev and agent-harness setup. Local stdio remains the MCP transport and the existing daemon remains the sole USB owner. Do not add a remote MCP service or systemd dependency. Homebrew/crates.io are secondary developer channels; add a `.deb` only after the release archive has user evidence, because package-level udev installation is its main additional value.

- [ ] **CLI-004.3**: crates.io publish workflow (PROT layers must be complete first)
- [~] **CLI-004.4**: Linux x86_64 cargo-dist release pipeline. The workflow validates its cargo-dist 0.32.0 plan, builds one combined archive containing both binaries, `SHA256SUMS`, licences/notices and `70-neural-dsp-cortex.rules`, and uploads it for pull-request/manual inspection. The post-gate CI path invokes auto-tag only after test, RustSec audit, Zizmor and REUSE pass; retries accept only an existing tag on the exact release commit, and release hosting repairs an existing partial release. Publication waits on the protected `release` environment. The archive check enforces a maximum glibc 2.34 symbol requirement and `libudev.so.1`; the docs-root installer verifies checksums and both staged binaries before replacing either installed program. **CI and clean-environment evidence 2026-08-25:** PR #29's Ubuntu 22.04 preview job built the archive; its published SHA-256 entry verified after download; an isolated stock Ubuntu 22.04 container with the documented `xz-utils` prerequisite installed both binaries, ran `cortex 0.1.0`, generated the offline schema and confirmed that `cortex-mcp` emitted no non-protocol stdout on EOF. **Live release evidence 2026-08-28:** gated main CI run `33183332396` created annotated tag `v0.2.0` at release commit `c2a7612`; the first host attempt stopped before publication on an incorrect flattened-artifact assumption; PR #36 fixed the path and added protected exact-tag recovery; approved run `33186261368` rebuilt that tag on Ubuntu 22.04 and published the GitHub Release. The public archive and `SHA256SUMS` downloaded successfully, the checksum matched `458f09aa8409ea7cde555358d7717d2fe3845a50a268cc3c74f879fe1ceca32f`, and the archive contained both binaries, the udev rule, notices and every licence text. **Remaining:** run the public installer against the published assets under CLI-004.10. Add Linux aarch64 only after build and HID behaviour are verified.
- [~] **CLI-004.9**: Guided `cortex setup` for non-developers. It diagnoses architecture, USB presence, canonical udev-rule installation, daemon health and paired MCP binary without opening HID. `--install-udev` uses narrowly-scoped explicit sudo and `--claude-code` registers the sibling absolute stdio binary at user scope. **Remaining:** clean-machine confirmation of effective hidraw access and Claude registration.
- [~] **CLI-004.10**: Test the published install path in a clean Linux environment: checksum verification, both binaries on `PATH`, completions, MCP process discovery, upgrade replacement of both binaries, and actionable no-device/udev diagnostics. **Candidate evidence 2026-08-29:** the first stock Ubuntu 22.04 run correctly refused installation because the public installer expected `cortex-mcp` to exit successfully on bare EOF, while the current server correctly reports a closed connection before initialization. The candidate installer now performs a real MCP `initialize` handshake. Against the published v0.2.0 archive and `SHA256SUMS`, an isolated stock Ubuntu 22.04 container verified the checksum and glibc/runtime contract, installed both binaries onto `PATH`, refreshed bash completions, discovered the sibling MCP path, returned actionable missing-udev/no-device guidance, completed an MCP initialization with no stderr, and replaced deliberately stale copies of both binaries and the completion on a pinned reinstall. **Remaining:** deploy the docs-root installer and rerun the exact public one-liner rather than the mounted candidate script.
- [ ] **CLI-004.11**: Add a `.deb` only after user demand is established. Its purpose is to install both binaries and the udev rule in conventional system locations; RPM remains demand-led rather than default scope
- [~] **CLI-004.12**: Keep the Windows door open. Done: `cortex-host` now exposes transport-neutral `LocalEndpoint`/`LocalListener`/`LocalConnection`; Unix socket paths, stale cleanup and owner permissions live only in its Unix adapter; daemon and MCP tests use the facade; `cortex-host` and `cortex-mcp` cross-check for `x86_64-pc-windows-gnu`. Outstanding: choose a maintained safe named-pipe dependency, implement current-user pipe ACLs and duplex byte streams, abstract detached process creation, then hardware-test the HID/session path on Windows
- [x] **CLI-004.13**: Added `cliff.toml`: git-cliff generates `CHANGELOG.md`; cargo-dist does not generate changelogs itself, it only optionally consumes an existing file (confirmed against cargo-dist's own `package.changelog` reference), so git-cliff is the one source. `s/version++` already called `git cliff --tag ... --output CHANGELOG.md` whenever `cliff.toml` existed but never checked it was installed; it now pins and verifies `git-cliff 2.13.1` during preflight, matching how `s/markdownlint` pins its tool. `.github/workflows/release.yml`'s `host` job now checks out `CHANGELOG.md` at the release tag and passes the exact `## [x.y.z]` section to `gh release create --notes-file`, falling back to `--generate-notes` only when that section does not exist yet (true until the next real version bump). The config was cross-checked against the `~/code/discourse/dsc` living example and verified by generating a real uncommitted preview against this repo's history with `git cliff --tag v0.1.1`; it correctly grouped `feat`/`fix`/`docs`/`nightly`/`chore`/`ci`, skipped `chore(deps...)` noise and non-conventional merge commits, and extracted the scope from `nightly(ID): ...` subjects. No `CHANGELOG.md` is committed yet because it is generated by the next `s/version++` release commit
- [ ] **CLI-004.14**: Publish unsigned Windows and macOS developer-preview archives as the next host expansion. Windows first requires the named-pipe transport, current-user ACLs, detached daemon lifecycle and native USB hardware smoke from CLI through MCP; macOS requires its local IPC/process lifecycle and the same hardware path. CI artifacts must be labelled provisional until that host has passed hardware smoke. Code signing, notarization and polished installers follow evidence and user demand rather than blocking the first explicitly unsigned previews.

### CLI-007: Shared machine contract

- [x] **CLI-007.1**: Added the `cortex-host::tool_registry` as one bounded canonical agent-operation registry shared by CLI and MCP. MCP discovery renders its tools from it; `cortex --schema --format json` prints the same names, descriptions, read-only metadata and JSON Schemas without opening a device. Registry tests enforce unique names, object schemas with `additionalProperties: false`, and the row-numbering warning on every row-taking operation. This satisfies CLI-002.3 without pulling host/runtime concerns into `cortex-rs`

## MCP (MCP)

The `cortex-mcp` MCP server for agentic patch editing. Its first non-persistent slice is implemented and hardware-verified through the held-session daemon; no save or delete tool is exposed.

### MCP-001: Safety surface

- [ ] **MCP-001.1**: Read and recall are free; saving is always explicitly confirmed
- [ ] **MCP-001.2**: Never write to the factory setlist; require one explicitly named USER target (1A-32H) and authorise only that target for the prepared operation
- [ ] **MCP-001.3**: Prepare every target and retain any backup **before working-copy edits begin**. A listing cannot prove emptiness because storage is eventually consistent; calling `read_preset` after edits recalls the target and destroys the grid being saved
- [ ] **MCP-001.6**: Implement and hardware-verify a restoration path before describing retained backup bytes as rollback. Candidate routes are device-side copy/import or keyed replay; an unkeyed whole-preset write is known to do nothing

### MCP-003: Show what the MCP server is for

The reason this project has an MCP server at all is that an agent can do things no editor UI can: take a plain-English brief, research what it means, and build the preset. That has to be demonstrated, not asserted.

- [x] **MCP-003.1**: The worked [tone-building demo](../docs/mcp-tone-building-demo.md) takes the brief "a basic 1987 GnR Slash tone" through live web research, live catalog selection and MCP grid construction. It recalls an operator-confirmed empty USER slot, places `Brit Plexi 100 Bright` and `412 Brit 60B GB 71 (M)`, read-backs every edit, and unconditionally recalls the empty slot without saving. The hardware smoke passed through the official MCP client on CorOS 4.0.1 on 2026-08-12. **The agent researches the web live rather than following a fixed recipe** - the research is the part worth demonstrating, and a canned recipe would show nothing an editor cannot already do. The demo therefore will not reproduce byte-for-byte, which is accepted.
- [x] **MCP-003.2**: The read-only `analyze_cpu_fit` tool joins the latest device CPU push to the live grid, reporting device-marked per-cell core assignment, aggregate two-core load, expensive cells, and conservative rerouting guidance. The [CPU-fit demo](../docs/mcp-cpu-fit-demo.md) corrects the unsafe assumption that four grid rows equal four cores: rows are routing lanes, while `is_on_core2` is the observed per-cell assignment. It makes no automatic edit or unverified row-to-core prediction. The official MCP-client hardware smoke passed on CorOS 4.0.1 on 2026-08-12 in 0.10 seconds, returning exactly two cores and the required row-to-core safety guidance without a grid edit.

### MCP-002: Tool surface

- [~] **MCP-002.5**: Non-persistent Capture/IR library and selection tools are implemented: shared schemas expose `list_captures`, `list_irs`, `set_capture`, and `set_ir`; the daemon fresh-lists and exact-matches `{key,name}` before selection, then fresh-reads the live grid before success. **Capture path HARDWARE-VERIFIED 2026-08-12:** an official MCP client listed an existing device Capture, selected it into USER `6A`'s unsaved grid, verified the exact selector string in the returned live grid, and unconditionally recalled `6A` for cleanup. **Exact residual:** run the equivalent MCP `list_irs`/`set_ir` smoke when an imported user IR is available; fresh strings alone do not prove the on-unit warning icon is absent
- [ ] **MCP-002.4**: Destructive tool: `save_preset`. Core PROT-009 correctness blockers and typed daemon failures are closed; remaining work is an MCP-held exact-target preparation-token registry, explicit confirmation, restoration semantics and an MCP save hardware smoke

## GUI (GUI)

The typed read/edit, live-cache, health, reconnect, and shared prepared-save foundations are in place. The interactive Tauri GUI has explicit fixture and daemon-backed modes with generation-checked Quad and Nano state plus bounded non-persistent working-state operations. Its production Rust boundary, physical unplug/reconnect behavior, automated DOM/IPC checks and dual-device rendered switching have passed; Tauri MCP automation remains outstanding. Save controls remain absent until the GUI implements exact-target preparation and confirmation UX, restoration semantics, typed failures and its own persistent-write hardware smoke. See [400-gui/spec.md](400-gui/spec.md).

**The prepared-save API is now enforced by the daemon and CLI.** Fixed in `743692a` (PROT-009.2): the daemon holds exact-target preparations under opaque tokens, and the CLI is two-phase with confirmation. A GUI that wants target backup and revalidation should call the same API.

The visual design goal is a **hardware-faithful rendering of the Quad Cortex front panel** - 10 footswitch/encoder positions, the colour OLED grid, scene LEDs, and the context strip - with wrapper panels (patch browser, block palette, parameter inspector, scene manager, IR/capture loader) alongside. Use Tauri MCP to tighten the feedback loop during GUI development.

### GUI-001: Scaffold and Tauri MCP

- [ ] **GUI-001.3**: Wire Tauri MCP for the dev feedback loop - drive the GUI from the MCP server to test Tauri commands without manual clicking
- [ ] **GUI-001.4**: Remove the temporary `RUSTSEC-2024-0429` audit exception when stable Tauri moves its Linux runtime from the unmaintained GTK3/glib 0.18 stack to GTK4/glib 0.20 or later. Tauri 2.11.5 is the latest stable release and still pins GTK3; upstream migration is tracked in [tauri-apps/tauri#12562](https://github.com/tauri-apps/tauri/issues/12562) and [PR #14684](https://github.com/tauri-apps/tauri/pull/14684). Do not force two incompatible glib generations or ship the GUI from an unreleased Tauri branch
- [x] **GUI-001.7**: Put an error boundary around the editing panels, so a frontend fault is visible instead of silent. **This has already bitten twice in one day:** a thrown React error unmounted `SceneSelector` while the daemon kept working, and it presented as "the keyboard stopped working" - writes were still reaching the device, so nothing looked broken except that the controls had gone. A Rules-of-Hooks violation in the parameter editor would have done the same. `gui/src/shared/ErrorBoundary.tsx` wraps the scene selector, grid and inspector panels independently in `App.tsx`, so one panel's fault does not blank the others; each fallback names the failed panel, shows the error message, and offers a reload rather than a blank space. `ErrorBoundary.test.tsx` proves it with a throwing test component: the fallback names the panel and shows a Reload control, `console.error` is not suppressed (React's own logging survives), and a second boundary alongside a failed one keeps rendering its own children. Browser-fixture verified at 1280x800 that wrapping the panels causes no visual regression in the non-throwing case. This is a frontend-only fault-isolation mechanism with no device interaction, so it carries no hardware dependency at all
- [~] **GUI-001.6**: Rust boundary tests cover readiness, Rust-owned scene conversion and no fixture fallback; both strict TypeScript modes build; browser fixture interaction was inspected at 800x600 and 1280x800. The production dashboard test passes against a real CorOS 4.0.1 held session. HARDWARE-VERIFIED 2026-08-09: unplugging hid the live grid and directory within one second while generation 1 became invalid; automatic recovery restored the same eight-block preset after about 10 seconds under generation 2. A second run exposed attempt/error details, accepted **Reconnect now** at attempt 4 and restored generation 2 in about three seconds; an offline timing test proves the signal interrupts a 10-second wait in under one second. Outstanding: use Tauri MCP from a client that exposes it for automated DOM/IPC checks
- [x] **GUI-001.8**: Quad and Nano now use product-scoped local endpoints, preserving one owner per physical device while allowing both sessions to remain warm concurrently. The GUI never shuts down one product to view the other, preserves explicit daemons, and keeps an already-running inactive auto-managed session alive through status polling. Quad and Nano also share semantic editor block cards, selected/operational states and inspector framing while retaining the Quad's 4x8 grid and Nano's fixed eight-role chain. HARDWARE-VERIFIED 2026-08-25 with both devices connected: both daemons reported connected concurrently and Quad to Nano plus the warm return to Quad each rendered in under one second; the previous return to Quad took 5-8 seconds

### GUI-002: Hardware-faithful control surface

- [ ] **GUI-002.1**: Render the Quad Cortex front panel: 10 footswitch/encoder positions, OLED grid, scene LEDs, context strip
- [ ] **GUI-002.2**: Footswitch interaction: click-to-press (toggle bypass / recall / navigate), drag-to-turn / scroll (adjust parameter), keyboard equivalents
- [ ] **GUI-002.3**: Mode-aware footswitch labels - reflect the current device mode (Preset / Stomp / Scene / Looper / Tuner)
- [ ] **GUI-002.4**: The OLED grid mirrors the device's live state (signal chain, block icons, bypass, active scene) from the crate's read paths
- [ ] **GUI-002.5**: Honest state - render what the device reports, not what the GUI thinks it sent

### GUI-005: Always-visible preset directory and CPU load

Stack confirmed as Tauri 2 + React + Mantine, as AGENTS.md says, unless a concrete reason to change appears.

Improve on the Cortex Control appearance while staying familiar enough to navigate without relearning.

### GUI-006: Screen-reader accessibility

[OpenCortex issue #10](https://github.com/VanIseghemThomas/OpenCortex/issues/10) records the concrete need for complete, independent Quad Cortex editing by a blind musician because the official visual editor does not expose a usable screen-reader surface. Treat accessibility as an architectural constraint from the first GUI scaffold, not a later audit. The hardware-faithful panel is one visual presentation of the domain model, never the only route to a control; keyboard equivalents alone are not acceptance.

- [~] **GUI-006.1**: Build the interaction model from semantic controls. Every control exposes an accessible name, role, current state, value, units, and available action; the signal chain has an ordered nonvisual representation with explicit row, column, routing, block type, and bypass state. Nothing depends only on colour, position, pointer drag, hover, or an icon. **First slices implemented:** the Quad grid is an ordered named group whose cells expose screen row, column, block type and bypass state; scenes use a radio group; Quad parameter controls expose their name, units and distinct slider/numeric actions; Nano FX cards are named pressed-state buttons operable by Enter/Space; Nano bypass, slider and Apply controls expose their target/state; and the header device selector is a named native button rather than a pointer-only badge. Semantic-query tests pin these contracts. Routing semantics and a complete role/value/action audit remain outstanding as the editing surface grows
- [ ] **GUI-006.2**: Provide screen-reader feature parity across the whole editing surface: browse and recall presets; inspect, place, move, replace, bypass, and remove blocks; edit every parameter in real units; manage scenes, routing, I/O and global settings; and review and confirm saves. Do not ship a reduced "accessible mode"
- [ ] **GUI-006.3**: Make asynchronous device behaviour understandable nonvisually. Announce device-originated state changes, command completion, refusals and silent-failure safeguards without flooding the screen reader; keep focus deterministic across dialogs, live updates and destructive confirmations; expose undo/dirty state wherever the protocol supplies it
- [ ] **GUI-006.4**: Make accessibility part of the release gate: automated semantic/accessibility checks plus manual end-to-end runs with actual screen readers and blind users. Establish Orca on the supported Linux webview as the Linux-first baseline during GUI scaffolding, then add NVDA and VoiceOver when Windows and macOS builds become supported; record the tested app, webview and screen-reader versions

### GUI-003: Wrapper panels

- [~] **GUI-003.1**: Patch browser - setlist/slot grid for quick preset switching, with search and favourites. The sidebar setlist/slot browser and immediate recall are implemented; search and favourites remain outstanding. **Recall HARDWARE-VERIFIED on CorOS 4.0.1, 2026-08-17:** selecting a sidebar preset recalled it on the unit, the daemon's echoed slot matched the requested slot, and the GUI re-read the resulting working copy rather than updating optimistically.
- [ ] **GUI-003.2**: Block palette - searchable list of available models from the `Catalog`, drag onto a grid cell
- [x] **GUI-003.3**: Parameter inspector - form-based editor for the selected block's parameters, showing real units (dB, ms, Hz) via catalog range conversion. **HARDWARE-VERIFIED on CorOS 4.0.1, 2026-08-17**: read a real block's parameters through the catalog join, wrote one, saw the device report the new value back, and restored the original (`editing_a_parameter_is_reflected_by_the_device`).

    **Measured, not assumed: stored float values are normalised 0..1, not the displayed figure.** A compressor's `THRESHOLD` reads `0.1458`, so every real-unit figure on screen is a catalog conversion. Nothing in the CLI or crate converted for display before this, so there was no existing convention to copy. The join happens in Rust - the webview never learns that the wire is normalised - and the catalog is fetched once and cached rather than pulled into the one-second poll, which parameters deliberately stay out of.

    The catalog drives the parameter list, not the stored values: a stored list can be shorter, and `Empty` entries occupy a wire index, so walking the catalog is what keeps an index addressing the parameter it names. `Meter` entries are shown and never editable; a degenerate catalog range yields no real value rather than a confident wrong one. Writes send the zero-based WIRE row, the daemon's `ParameterWrite` echo is compared against the index requested, and the control re-reads afterwards rather than assuming.

    **The same accessibility fault appeared for the third time and was finally understood.** Disabling a control while its own async write is in flight strands focus on the document, because a disabled element cannot hold focus - measured here as five arrow presses moving a slider one step instead of five, with `document.activeElement` on `BODY` after the first. It cost three hardware rounds on the scene selector and recurred immediately in the parameter editor. Controls now stay enabled and announce `aria-busy`. A related trap: re-reading after each write resets the control's draft value and fights the user's input, so device sync is suppressed while a control is being held.

    A React Rules-of-Hooks violation was also caught in browser mode before reaching hardware - a `useEffect` placed after an early return changes the hook count between renders and throws, which presents as a panel that silently stops working rather than an obvious failure. There is still no error boundary around the panel.

    **Block bypass is in the same panel, offline-verified only.** A switch bypasses or engages the selected block through `Request::SetBypass`, which the daemon answers with both `applied` and `verified` - it reads the cell back - and anything short of both is treated as failure rather than a silent no-op. Like every other edit here it reaches the ACTIVE scene only, because that is how the device stores bypass, and the control says so. Browser-fixture verified: toggling flips the switch, and the grid cell picks up its dashed/dimmed styling and `bypassed` text from the re-read rather than optimistically. **Hardware follow-up needed:** confirm a GUI bypass is audible and matches the unit's own display.

    Outstanding: parameter search/grouping for models with many parameters, and per-scene editing (a write currently reaches the active scene only, which is what the badge says).
- [~] **GUI-003.4**: Scene manager - copy/swap/relabel/recolor scenes without the footswitch mode dance. Expose a full RGB picker rather than reproducing the unit's fixed palette: CorOS 4.0.1 stored and freshly read back off-palette `0xFF808080` exactly, although physical LED rendering still needs visual confirmation.

    **First slice HARDWARE-VERIFIED on CorOS 4.0.1, 2026-08-16.** Scene *switching* is wired end to end: a `switch_scene` Tauri command routes to `Request::SwitchScene`, and `LiveSnapshot` now carries all eight scenes. The zero-based index and the displayed A-H letter are both produced in Rust (`scene_snapshots`) so the mapping has one implementation; the webview sends the index and never the letter. Out-of-range indices are refused in the Tauri boundary before reaching the daemon, and the fixture mirrors that refusal so browser mode cannot make a rejected interaction look workable. All eight scenes are always offered, including ones the preset never labelled, because an unlabelled scene is still reachable on the unit.

    Nothing is updated optimistically: the switch is followed by a re-read, so the GUI shows the scene the device reports (GUI-002.5). Built as a real radio group rather than styled buttons, per GUI-006.1 - a screen reader gets set size, position and current selection for free, the letter and label carry the state as text so nothing depends on colour, and arrow-key navigation drives real switches. Browser-fixture verified at 800x600 and 1280x800: no horizontal overflow, wraps 3 rows then 2, keyboard switching confirmed D->E, both out-of-range refusals confirmed with state unchanged. **Measurement found a real accessibility defect:** Mantine's default radio is 20x20, under the 24x24 CSS px of WCAG 2.2 AA 2.5.8, so the control is pinned to `size="md"`.

    **Hardware testing found a real bug that every offline test missed.** The daemon answers `SwitchScene` with `{"scene": N}`, not the bare acknowledgement the GUI decoded, so every switch failed with `decoding cortex session response`. The fixture could not have caught it - it does not speak the daemon protocol - and neither could the unit tests, which drive the source trait rather than the wire. The reply is now decoded and its echoed scene compared with the one requested, so a session that switched to a *different* scene is an error rather than a GUI quietly showing the wrong thing. Two ignored hardware tests now cover it: `switching_scenes_is_reflected_by_the_device` proves the device reports back the scene the GUI asked for, and `externally_originated_scene_changes_reach_the_gui` proves a change made by another client appears in the GUI, which is what stops the GUI trusting its own memory over the device. Both restore the scene they found.

    **Operator-assisted verification, CorOS 4.0.1, 2026-08-16.** Pressing scene footswitches on the unit moved the GUI's radio selection and its scene readout within about half a second, with no GUI interaction; an independent poller recorded all eight scenes being exercised, so the GUI is following the device rather than its own memory. **Arbitrary scene RGB does render on the physical LEDs** - stepping through the eight scenes showed the reported colours on the unit, which answers the open question in favour of a full RGB picker rather than reproducing a fixed palette. Visual inspection confirms the hue, not the exact byte value, and that is as far as an eye can take it. Orca announced the scene on each change.

    Two transient `active_scene: None` reads were observed during the session, each recovering within the same second. Not diagnosed; recorded here rather than explained, because a guess about a cache transient is worth nothing.

    **Keyboard navigation cost three hardware rounds and the cause was mine, not the engine's.** The first suspicion was that WebKitGTK does not implement radio-group arrow keys; explicit WAI-ARIA key handling was added, which is worth keeping because it makes behaviour identical across engines instead of engine-defined. But the actual fault was `disabled={disabled || busy}`: **a disabled input cannot hold focus**, so disabling the control while its own switch was in flight threw focus onto the document and killed every subsequent key - including after a mouse click, which is why clicking did not "re-enable" it. The control now stays enabled, announces `aria-busy`, and guards re-entry in the handler instead. Focus is also restored after React commits rather than inside the handler, because re-rendering replaces the input nodes.

    **The browser test had been flattering the code.** It passed only because it called `.focus()` programmatically first, which no user does. Driving a real mouse click on the label reproduced the hardware fault in Chromium immediately - the bug was never WebKitGTK-specific. A test that sets up the state the implementation happens to need is not evidence.

    Verified on hardware after the fix: click then three consecutive right arrows walk C-D-E-F with the unit following, left/Home/End all work, and focus survives the one-second dashboard poll. Orca announces each scene.

    Two loose threads, recorded rather than explained: transient `active_scene: None` reads (seen five times across the session, each recovering within the same second), and occasional "Refresh failed" banners that were markedly less frequent after a clean GUI restart. Neither is diagnosed. A React error thrown by this component mid-session also unmounted it while leaving the daemon working, which is worth knowing because it presents as "the keyboard stopped working" while writes still reach the device - there is no error boundary around the panel yet.

    **Relabel and recolour are implemented, offline-verified only.** The active scene can be renamed and given any RGB colour, using a full picker rather than the unit's eight-colour palette - which the hardware pass above justifies, since arbitrary RGB was confirmed to render on the physical LEDs. A blank name clears the label rather than writing an empty string, because the unit stores a one-space value for unlabelled and `""` would invent a third state; alpha is forced opaque, since an LED cannot be transparent and a zero alpha would silently read as black. Both are tested in Rust, and the daemon's echoed scene index is compared with the one requested. Browser-fixture verified: rename propagates to the radio label and the header, blanking returns the scene to "unlabelled", and an off-palette `0x808080` round-trips as `0xFF808080`. **Hardware follow-up needed:** confirm a GUI rename and recolour appear on the unit, and that an off-palette colour renders on the physical LED as it did when read back.

    Outstanding: copy and swap; an error boundary so a component fault is visible rather than silent (now [GUI-001.7](#gui-001-scaffold-and-tauri-mcp)).
- [ ] **GUI-003.5**: IR / Capture loader - file-browser-style access to the device's captures and IRs
- [ ] **GUI-003.6**: Make externally originated live-grid edits legible in the GUI. Diff successive generation/revision-checked device snapshots so MCP/CLI block placements visibly enter the grid and parameter changes can focus or pulse the affected block's inspector without a USB panel command. Device read-back remains ground truth; do not steal focus while the user is actively editing, and do not guess one target when a snapshot changes several cells

### GUI-004: Safety surface and governance

- [ ] **GUI-004.1**: Reuse the MCP safety surface (factory refusal, exact target, pre-edit target preparation/backup, explicit confirmation, trap-surfacing) for save actions. If an occupied target was not prepared before the grid became dirty, require the user to choose and prepare another target rather than recalling it and losing the edits
- [~] **GUI-004.2**: The first slice defines the typed capability matrix in `gui/src-tauri/src/capability.rs`: `CapabilityStatus` (`confirmed-readable`, `confirmed-writable`, `inferred`, `unsupported`, `unverified`) defaults to `unverified`, and `CapabilityMatrix::status` returns that default for any operation not seeded - proven by a test keyed on an operation nobody has added yet, and by a second test proving an *empty* matrix confirms nothing. `default_matrix()` is seeded only from GUI operations this file records as hardware-verified: `switch_scene` and `recall_preset` (GUI-003.4 2026-08-16 and GUI-003.1 2026-08-17, both `confirmed-writable`), and `block_parameters`/`set_parameter` (GUI-003.3 2026-08-17, `confirmed-readable`/`confirmed-writable`). A dedicated test proves `set_bypass`, `set_scene_label` and `set_scene_color` - implemented and offline-verified per GUI-003.3/GUI-003.4, exactly the trap this matrix exists to catch - stay `unverified`, because no hardware pass has confirmed them. **Outstanding:** expose the matrix through the Tauri boundary and render the labels in the webview; the governing GUI spec requires labels in the UI, so defining the backend type alone does not complete this item
Version synchronization is completed and tracked canonically under ENG-001.2.
- [x] **GUI-004.4**: `docs/gui/first-draft.md` documents what the GUI actually does today: fixture vs daemon-backed mode and how to choose one, `s/gui-dev`, the sidebar preset directory and recall, the scene selector (switch/rename/recolour), and the parameter inspector (bypass and parameter read/write/read-back), each stated as non-persistent - recall replaces the working copy, scene and parameter edits reach the device but save nothing - and with no save control described, since none exists. The page was already in `mkdocs.yml` nav; `zensical build` renders it with no issues. This is documentation only and carries no hardware dependency

## Engineering (ENG)

### ENG-001: DX and testing

- [x] **ENG-001.3**: `s/install-hooks` installs `.githooks/pre-commit`, which runs `s/lint` (about 6 seconds with a warm cargo cache). Installation stays explicit and opt-in per checkout - no other script installs it, so a clone never silently gains a commit-time side effect - and `s/install-hooks -u` reverses it. Sets `core.hooksPath` rather than copying into `.git/hooks`, which Git does not track and which goes stale as soon as the tracked hook changes
- [x] **ENG-001.4**: Markdown lint via `markdownlint-cli2`, pinned to 0.23.2 and run through `npx` by `s/markdownlint`, wired into both `s/lint` and CI. Config is `.markdownlint.jsonc`, adapted from the house-style template with each divergence carrying its reason inline: `MD049`/`MD050` follow the repo's existing asterisk emphasis rather than the template's underscore, and `MD046` is off because every hit was a false positive - a *fenced* block correctly indented inside a Material/Zensical admonition or content tab, which markdownlint does not parse. `MD060` is pinned to `compact` because the default `any` reports against whichever style is the closest match per table, which gave inconsistent findings. **The lint paid for itself immediately:** `MD056` found three rows in `spec/200-cli/spec.md` where an unescaped `|` inside a code span (`switch|label|unlabel|...`) split the row into 8 cells in a 3-column table, silently truncating the requirement text in the rendered docs. Existing prose was not reflowed; the remaining changes are whitespace only, verified with `git diff -w`
- [x] **ENG-001.7**: Added documentation quality checks for the deployed Zensical site. The dependency-free `s/check-docs-nav` validates pages, nav entries and named exceptions in both directions. `s/linkcheck` builds the site and validates authored links, images and anchors against rendered HTML within the configured deployment prefix, so title-bearing and reference links, inline code, Unicode, duplicate headings and explicit IDs follow Zensical rather than a source-text approximation. `s/spellcheck` requires the reviewed codespell pin in `requirements.txt` and never mutates the caller's Python environment. All three checks run from `s/lint` and dedicated CI steps; `s/test` and CI run isolated fixtures for their success and failure cases. Measured against the reviewed tree: 13 docs pages, 0 orphans, 2,735 rendered internal targets and 0 spelling findings. The implementation follows the house-style documentation standard. **Hardware follow-up: none** - offline docs tooling only
- [x] **ENG-001.5**: Closed the documented local/CI parity gap where practical: `s/lint` now runs `cargo clippy --all-targets --no-default-features -- -D warnings` (workspace-wide, replacing the narrower `cortex-rs`-only `cargo check`) and `s/test` now runs `cargo test --all --no-default-features`, mirroring the two CI-only no-default jobs exactly. The Windows host/MCP cross-checks remain CI-only since they need a cross toolchain not guaranteed to be present locally

### ENG-002: CI

Release workflows live under CLI-004; this section tracks non-release CI gaps only.

- [x] **ENG-002.2**: `npm run check --prefix gui` (fixture + Tauri frontend typecheck/build) was already in CI; the audit found the actual gap was the Tauri backend itself never running through the Tauri CLI. Added a `Tauri build boundary (debug, no bundle)` step running `npm run tauri --prefix gui -- build --debug --no-bundle --ci`, which drives `tauri-build`'s `tauri.conf.json`/icon parsing and the `beforeBuildCommand` frontend build that plain `cargo test`/`cargo clippy` never exercise. No new GitHub Action was needed, so no new SHA pin. Native Windows/macOS jobs remain future work behind those hosts' support

### ENG-003: Governance

- [ ] **ENG-003.3**: If a closed derivative ever needs to exist, add `DUAL-LICENSE.md` and the boilerplate. Requires approval
- [x] **ENG-003.4**: `SECURITY.md`, `CONTRIBUTING.md` and `CODE_OF_CONDUCT.md` are in place, with GitHub issue and pull-request templates, adapted from the house-style new-repo community-health templates rather than written from scratch. All three carry this project's specifics rather than the template's clinical defaults: private vulnerability reporting through GitHub advisories, the local-USB risk surface (malformed device responses, local socket access to the held session, MCP tools invoked without confirmation), the device-data redaction rule, the expectation that protocol changes are hardware-verified or marked provisional, and a pointer to the prior-art licensing boundaries in `AGENTS.md`
- [ ] **ENG-003.5**: If we ever target on-device builds, adapt `qc-stomp-tools` (MIT) with attribution and a NOTICE entry

### ENG-005: `s/usb-trace` - observe Cortex Control on the wire

A script that sets up passive USB observation of the official Cortex Control app driving the device, so its traffic can be decoded against our schema. This is the tool for questions of the form "how does the official client do X" - the answer to which is evidence about the wire, not inference about intent.

**It paid for itself before Cortex Control was ever traced.** The first capture was of our OWN client, run only to check whether the device's apparent silences were real or an artefact of our RX path. It showed the bus idle for 46.8 s in the middle of a handshake, with the device answering every write in 217 us - which identified writer starvation in `session.rs` (PROT-008.5, PROT-008.6.10) after a full day of attributing that variance to the device. Two features had already been built and withdrawn on the strength of the wrong explanation. Trace our own side first; it is cheap, needs no VM, and the bug is as likely to be ours.

**Named `usb-trace`, not `usb-record` or `usb-capture`, deliberately.** "Capture" already means a Neural Capture in this domain and "record" implies audio; either would suggest this script records sound, which it emphatically does not. `trace` is already the project's word for protocol observation (`CORTEX_TRACE`), so the two read as the same idea at different levels.

The method is documented in the [protocol reference](../docs/protocol.md#observing-the-wire): with the QC passed through to a Windows VM under QEMU, the **host** kernel still sees the traffic. So `modprobe usbmon` plus a capture of the relevant `usbmonN` interface on the Linux host records everything, without needing USBPcap inside Windows and without the macOS exclusive-access problem.

- [ ] **ENG-005.4**: Optionally a Wireshark Lua dissector, since Wireshark's built-in protobuf support can be pointed at our vendored `.proto` files - worth it only if the GUI earns its place alongside `s/usb-decode --live`
- [ ] **ENG-005.3**: Runbook: what to do in Cortex Control while tracing to answer a specific question, starting with capture export (PROT-007.1)

**Known obstacle.** The QEMU/Windows/Cortex Control setup on the development machine works but drops its connection regularly, so it is adequate for short targeted observations and not for sustained work. Plan traces as single short scripted actions - "open the app, export one capture, stop" - rather than long exploratory sessions, and expect to repeat them.

**Do not commit raw captures.** They contain readable preset, path, device, and build strings. Commit decoded findings in our own words, as the prior art does. See the legal hygiene section of AGENTS.md.

### ENG-006: `s/hardware-smoke` - scripted CLI smoke test with read-back assertions

- [~] **ENG-006.2**: Daemon start/status/stop and routed read/edit/save paths are scripted and hardware-verified. Physical unplug/replug was manually verified on 2026-08-07; only a safe automated reconnect substitute remains optional

### NANO-001: Nano Cortex over USB HID

- [x] **NANO-001.1**: Hardware discovery established VID:PID `152A:88E7`, HID interface 5, input/output report IDs `0x01`/`0x02`, 64-byte report bodies, shared length/flag framing, and shared `FIRST`/middle/`LAST` reassembly. The known read-only Nano current-state command returned nine USB HID reports and the existing decoder recovered firmware, control, assignment, bypass, and FX-model state. Bluetooth ownership produced an explicit `Device is busy!`; the same request succeeded after Bluetooth disconnected. Raw captures remain private because their padding and payload contain device and user strings
- [x] **NANO-001.2**: Hardware-verified 2026-08-12 on a real Nano Cortex with Bluetooth disconnected. Repository code opened PID `0x88E7` with 65-byte geometry, sent the read-only current-state request, and reassembled a multi-report response (10 reports: FIRST + 8 middle + LAST, 574 bytes) with correct flag-driven reassembly. The state body size varies with device content; the original 2026-08-11 probe measured 9 reports / 546 bytes. `Transport::request` and `Session::open` both reject Nano with `UnsupportedDeviceOperation` before USB I/O. Quad regression passed immediately after on the same machine: direct version read, correlated session version read, and a full held session (start/status/grid-show/stop) all healthy on CorOS 4.0.1. One canonical byte-checked udev rule covers both products
- [x] **NANO-001.3**: Added the Nano-specific four-byte-footer codec and serialisable typed current-state model behind the common HID frame layer. The model preserves absent controls as unknown and exposes the fixed signal chain as eight ordered roles (Gate, Pre FX 1-2, Capture, IR/Cab, Post FX 1-3), without forcing it into the Quad grid. Fictional fixture tests cover exact request bytes, strict malformed input, shared frame reassembly, all eight roles and JSON round-trip. **HARDWARE-VERIFIED 2026-08-18:** a fresh read-only USB request reassembled 10 reports / 594 meaningful bytes and decoded all eight roles with all five amp controls present; no state strings or identifiers were printed. The decoder/field map adaptation is attributed to `rixrix/deskop-nano-cortex` (Apache-2.0) and its credited source `choldy/nano-cortex-web-editor` (MIT) in `NOTICE` and `THIRD-PARTY-NOTICES.md`
- [x] **NANO-001.4**: The held daemon and CLI support explicit Nano selection with `cortex session start --device nano`, protocol-v19 status reports `device_kind`, and `cortex nano state` returns the typed fixed-chain snapshot. Product-scoped local endpoints enforce one owner per physical device while allowing Quad and Nano sessions to run concurrently; each daemon refuses operations for the other product. **HARDWARE-VERIFIED 2026-08-18:** detached start, status, repeated daemon-backed state reads, wrong-device refusal and clean stop passed against the connected Nano. Measurement found that an immediate duplicate state request times out and the next succeeds; a six-second interval succeeds, so the daemon caches the decoded startup state and refreshes no more often than every five seconds while serving faster polls from memory. Bluetooth ownership is also typed: the measured 23-byte confirmation has footer `85 00 00 00`, field 1 varint `3` and field 4 exact text `Device is busy!`; the decoder validates the whole shape rather than substring-matching. With the phone app connected, the hardware smoke returned `DeviceBusy` in 0.07 seconds and daemon startup surfaced the ownership conflict directly
- [x] **NANO-001.5**: The GUI consumes the same `NanoCurrentState` through the daemon and renders the eight fixed roles as one responsive row (Gate, Pre FX 1-2, Capture, IR/Cab, Post FX 1-3) plus explicit Apply controls for the five raw amp values. It does not force Nano state into Quad rows/scenes/presets. The Tauri backend reuses or launches product-scoped auto-managed daemons; the header selector switches between warm Quad and Nano sessions without stopping the inactive product. **Hardware-verified backend 2026-08-18:** the Tauri dashboard boundary read all eight roles through a real Nano daemon in 0.02 seconds from the paced held snapshot; a Tauri amp write changed Gain by one raw step, independently read it back, restored it and verified restoration in 12.15 seconds. Fictional Nano fixture mode at `?device=nano` passed responsive browser checks and an Apply interaction. MCP exposes `read_nano_state` and `set_nano_amp`; Quad tools invoked against a Nano daemon fail at the device boundary rather than pretending compatibility. The Tauri raw FX backend boundary was hardware-verified on 2026-08-21 under NANO-001.7. An official MCP client discovered and exercised all five Nano tools against hardware on 2026-08-21. The rendered native Linux FX control also passed a reversible slider edit/read-back/restoration smoke. Dual-device selection and warm switching were hardware-verified on 2026-08-25 under GUI-001.8
- [~] **NANO-001.6**: Nano working-state writes are being added one operation family at a time, with exact typed builders, hardware read-back through a separate state request, and unconditional restoration where a test changes a value. **Amp controls hardware-verified 2026-08-18:** Gain, Level, Bass, Mid and Treble each accepted a typed same-value write and independently read back exactly on one held connection in 30.28 seconds; a reversible Gain test also changed and restored the value. **Bypass hardware-verified 2026-08-20:** the earlier "second bypass write ignored" trap (2026-08-18) could not be reproduced across Pre FX 1, Pre FX 2, Post FX 1 and Gate, including a rapid-fire test sending all four toggles in quick succession. The device sends an immediate `73 00 00 00` footer acknowledgement after each bypass write, and a fresh state read after the measured six-second settle confirms the new value. Bypass is now exposed through daemon, CLI, MCP and GUI with the same read-back verification as amp writes. One decoder caveat: the Gate's "on" state is represented by the absence of field 54, so the decoder reports `bypassed = None` when the gate is on rather than `Some(false)`. **Raw FX parameters hardware-verified 2026-08-21:** all five editable slots returned non-empty normalized vectors; one interior Pre FX 1 value changed by `0.01`, read back within `0.001`, restored on a fresh transport and read back again; daemon-backed CLI and Tauri-backend same-value writes also passed fresh read-back. An official MCP client discovered the complete Nano tool set, decoded typed state and passed same-value amp, bypass and FX write/read-back checks in 14.40 seconds. The rendered native Linux control then changed one Pre FX 2 slider by `0.001`, displayed device-confirmed convergence, restored the original through the same control and displayed matching device/draft values. The FX response has no slot or request ID, so serialized callers cannot distinguish a delayed older reply from the requested slot. Protocol v18 exposes verified amp, bypass and raw FX parameter writes plus the hardware-provisional Gate-reduction write. Do not expose save, preset switching, model replacement, Capture/IR selection or other unverified writes merely because prior art documents a frame

**NANO-001.6 Gate-reduction update, 2026-08-22 and 2026-08-26:** the exact 0-100% builder, daemon/CLI/MCP/Tauri routes, GUI Apply control, strict range rejection and fresh state confirmation are offline-tested. On both hardware attempts the connected unit omitted optional state field 53, which means unknown rather than 0%; the reversible smoke therefore refused before any write because it could not retain a trustworthy original value. This operation remains hardware-provisional until a readable original is changed, independently read back, restored and verified.

## Future

- **FUTURE-001 (promoted)**: Nano Cortex hardware verification and USB support are active under NANO-001; preserve this ID as the original umbrella rather than counting it as a separate future item
- **FUTURE-002 (promoted)**: Nano Cortex application-protocol reuse is active under NANO-001.3; preserve the Apache-2.0 and indirect MIT attribution boundary documented in [prior-art.md](prior-art.md#an-additional-project-not-vendored)
- **FUTURE-003 (promoted)**: Cross-platform GUI completion is now active under GUI-001 through GUI-006 and CLI-004.12; preserve this ID as the original umbrella rather than counting it as a separate future item
- **FUTURE-004**: On-device builds (qc-stomp-tools ioctl route) - only if there is a compelling reason; the USB route is preferred
- **FUTURE-005**: Protocol-version probe - surface a CorOS version check rather than hard-coding assumptions, since the protocol has no version field on the wire
- **FUTURE-006**: Conformance suite - port pyquadcortex's offline test suite as a Rust integration test reference
- **FUTURE-007**: Audio feedback loop - let the MCP server "hear" the unit. The Quad Cortex presents class-compliant USB **audio** interfaces that are separate from the HID interface we use, so a host could play a standardised stimulus (DI guitar phrase, sine sweep, impulse) through the chain and capture the processed result **without contending for the exclusive HID connection**.

  Confirmed on hardware 2026-08-02: of the unit's six USB interfaces, **0 through 4 are Audio class (class 1) and only interface 5 is HID (class 3)**. ALSA already enumerates the device as a working card (`USB-Audio - Quad Cortex`) with no driver work required, so the capture side of this needs no reverse engineering at all - it is an ordinary audio device that happens to also speak our HID protocol on a different interface. Comparing captured output against a dry reference would characterise what a chain is doing to the signal.

  Worth being precise about what this buys, because it is not what it first appears. An agent editing a patch already has **ground truth** available: `read_current_preset` returns the actual grid, so "did my edit land on the right block" is answerable today by read-back, and audio analysis is a strictly worse way to answer it. What audio adds is **aesthetic and perceptual judgement** - "is this too dark", "is the gain staging sensible", "does this sound like the reference tone" - which read-back cannot answer at all.

  So this is not a correctness or safety mechanism and should not be treated as one; it is what would let an agent iterate on *tone* rather than on *structure*. That is genuinely novel and nobody has built it, but it is a substantial subsystem (audio I/O, latency alignment, feature extraction, a perceptual similarity metric) and it should not start until the grid-edit surface it would be judging actually exists.

  Open questions: does the QC expose a usable dry/wet split over USB (there is a `dry_wet` field in `USBPortSettings`) so a dry reference can be captured simultaneously rather than in a separate pass; and what stimulus set is both compact and discriminating enough to be worth standardising.

- **FUTURE-008**: Host-owned Neural Capture lifecycle. This is explicitly descoped, not a release blocker: Capture creation, dialog handling, transfer, backup, and cloud workflows remain native-device workflows (PROT-006.17 and PROT-007). On CorOS 4.0.1, replying to `NeuralCapture{try_to_show_dialog: true}` with the exact false-only `NeuralCapture{UPDATE, show_dialog: false}` made the unit freeze, stop answering HID, and reboot; causation is unestablished, but the response is unsafe, removed from public code and tests, and must not be retried. Remaining silent suppresses the on-unit flow. Do not promote this item without a new product decision, a complete safe host workflow, and a hardware safety plan.
