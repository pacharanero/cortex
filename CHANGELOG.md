# Changelog

All notable changes to this project are documented here, generated by
[git-cliff](https://git-cliff.org) from Conventional Commits. Do not hand-edit
past entries; re-run `s/version++` (or `git cliff`) instead.

## [0.2.0] - 2026-08-28

### Features

- *(cli)* Invert persistent dry-run behavior
- Add uniform dry-run and live GUI reads
- Add verified scene management
- *(gui)* Add actionable reconnect recovery
- Add verified block movement
- Expand verified Cortex control surfaces
- Verify grid writes and Nano HID transport
- Device-neutral HID framing and Nano write path
- Hardware-verify Nano transport and Quad regression
- Share agent schemas across CLI and MCP
- Select device Captures through MCP
- Hardware-verify MCP-003.1 tone-building demo
- Analyze Quad Cortex CPU fit through MCP
- Publish Linux archives and guide setup
- *(gui)* Switch scenes from the GUI (GUI-003.4 first slice)
- *(gui)* Recall presets from the sidebar and colour the grid
- *(gui)* Edit block parameters in the inspector (GUI-003.3)
- *(gui)* Rename and recolour scenes (GUI-003.4)
- *(gui)* Bypass and engage blocks from the inspector
- *(nano)* Decode the typed current state (NANO-001.3) (#14)
- *(nano)* Held daemon, CLI, MCP and GUI for Nano state and amp writes (NANO-001.4, NANO-001.5, NANO-001.6) (#15)
- *(nano)* Expose verified Gate/FX bypass through daemon, CLI, MCP and GUI (NANO-001.6) (#16)
- *(gui)* Device selector for switching between Quad and Nano (NANO-001.5) (#17)
- *(nano)* FX parameter read and write through daemon, CLI, MCP and GUI (NANO-001.6) (#19)
- *(nano)* Add held-session editing and audit corrections (#28)
- *(gui)* Unify dual-device editor surfaces (#29)
- *(nano)* Resolve effect model names
- *(nano)* Add guarded FX parameter descriptors (#33)

### Bug Fixes

- Remove unsafe capture-dialog response
- *(ci)* Permit release workflow hosting
- *(gui)* Decode the real scene-switch reply (GUI-003.4)
- *(gui)* Keep scene controls focusable while switching (GUI-003.4)
- *(gui)* Keep capability labelling partial
- *(gui)* Suppress auto-refresh during Nano writes, return cached state when busy (#18)
- *(dist)* Document preview install prerequisites (#30)
- *(traceability)* Enforce Rust header coverage (#31)
- *(client)* Refuse ineffective tuner visibility
- *(release)* Harden publication and installer (#34)

### Overnight Automation

- *(ENG-002.2)* Add Tauri build boundary to CI
- *(ENG-004.1)* Complete @see traceability for 300-mcp zone
- *(GUI-004.2)* Typed capability matrix for hardware-verified vs provisional labelling
- *(GUI-001.7)* Per-panel error boundary for the GUI editing surface (#12)
- *(GUI-004.4)* Document the GUI's preset directory, scene selector and parameter inspector (#13)
- *(ENG-001.7)* Docs-site quality checks (nav, links, spelling) (#27)
- *(CLI-004.13)* Wire git-cliff changelog generation into release flow (#32)

### Documentation

- Define overnight agent routine
- Complete PROT-006.11 and descope host capture lifecycle
- Add community-health files per house style
- *(roadmap)* Restock the overnight queue and record two new gaps
- *(ENG-004.1)* Trace crates/cortex-host/src to its owning zone spec (#20)
- *(ENG-004.1)* Trace GUI backend to its owning spec (#21)
- *(ENG-004.1)* Trace remaining Rust files
- *(nano)* Record repeated gate baseline omission

### Miscellaneous Tasks

- *(dx)* Guard GUI manifest version sync
- *(dx)* Run no-default workspace clippy/tests locally (ENG-001.5)
- Add Linux release preview pipeline
- *(dx)* Add Markdown lint and tracked Git hooks (ENG-001.3, ENG-001.4)
- *(ENG-004.2)* Enforce traceability links

## [0.1.0] - 2026-08-07

### Features

- USB HID protocol stack, CLI, model catalog, grid editing, and docs
- *(cli)* Hold one session in `cortex connect`, and refuse to collide with it
- *(cli)* Route `version` through the daemon; withdraw the silence fail-fast
- *(s)* Add s/usb-trace for observing the device on the wire (ENG-005.1)
- *(s)* Decode a USB capture into Cortex Control messages (ENG-005.2)
- *(session)* Fail fast when the device stops talking (PROT-008.6.4)
- Version READ in the handshake, CpuLoad subscribe, keepalive fix
- Live DSP load, and a generic protobuf dump that found why (ENG-005.3)
- *(cli)* Route presets, recall and catalog through the daemon (008.6.7)
- *(cli)* Route grid edits through the daemon
- *(s)* Add s/version++ (CLI-004.1)
- *(cli)* Restructure around the QC's own nouns (CLI-005)
- *(cli)* Worked examples in every command's help (CLI-006)
- *(cli)* `session start` runs in the background
- *(crate)* Save the working grid to a slot (PROT-006.10, partial)
- *(crate)* Delete_preset, and hardware-verify both file operations
- Subscribed state cache, prepared saves, and wider daemon routing
- *(gui)* Add interactive Quad Cortex shell
- *(mcp)* Add daemon-backed live-grid tools
- *(distribution)* Add agent onboarding foundation
- *(host)* Add portable local IPC boundary
- *(storage)* Add verified preset file operations

### Bug Fixes

- *(session)* Yield the device lock so a writer cannot starve
- *(session)* Do not treat the device's heartbeat as unfinished state
- *(s/usb-trace)* Correct the capture-permission advice
- *(s/usb-trace)* The usbmon check failed only when it succeeded
- *(session)* Stop the RX loop starving writers
- *(decode)* Read captures taken through VM passthrough
- *(session)* Keepalive every 1s, not 5s - the idle silence was ours
- *(cli)* Say when a catalog search finds nothing; hide empty folders
- *(reuse)* Keep queries.md on the repo's actual doc licence
- Wire prepared saves into the CLI and daemon (PROT-009.2, .4, .10, .11)
- Validate named parameter value types
- Invalidate cache on malformed envelopes
- Invalidate partial file listings
- *(gui)* Ignore generated Tauri schemas
- *(core)* Harden recovery and save safety
- *(ci)* Install Tauri Linux prerequisites
- *(ci)* Install hidapi Linux prerequisite

### Documentation

- Confirm on-unit edits push, and design `cortex connect` around it
- *(roadmap)* Record PROT-008.6 state, and correct the `version` plan
- *(roadmap)* Correct 008.6.10 - DSP load is the likelier cause
- *(roadmap)* 008.6.10 is variance, not degradation - and likely 008.5
- *(roadmap)* Retrying a listing READ makes it worse - measured, reverted
- *(roadmap)* PROT-008.5 and 008.6.10 resolved - the cause was ours
- *(protocol)* Document what the wire actually does
- *(roadmap)* Record which commands now route through the daemon
- *(roadmap)* Close CLI-005 and CLI-006
- *(roadmap)* Record the answered queries, retire queries.md
- *(roadmap)* Split outstanding from finished, and fix three stale entries
- Record prior art, and file the review's correctness gaps
- *(roadmap)* Confirm PROT-009.2 by call-site search
- Mark PROT-009.2/.4/.10/.11 fixed and regenerate CLI reference
- *(NOTICE)* Broaden pyquadcortex attribution to cover protocol derivation
- Record first hardware smoke findings
- Foreground cross-platform GUI toolkit

### Performance

- *(session)* Stop paying for a full state dump on every command
- *(session)* Wait for the catalog before sending the rest of the handshake

### Refactor

- *(crate)* Move the preset views out of the CLI (PROT-004.1, PROT-004.3)

### Testing

- *(session)* Fake transport, and the latent bug it found (ENG-001.y)
- *(daemon)* Exercise the socket protocol over a fake session
- Add hardware smoke coverage

### Miscellaneous Tasks

- *(licensing)* Prose is CC-BY-SA-4.0, code stays AGPL (approved)
- Auto-tag on version bump (CLI-004.2)
- *(gui)* Make Tauri feature defaults explicit
- Harden dependency and docs automation
